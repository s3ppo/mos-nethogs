#!/usr/bin/env bash
set -euo pipefail

INSTALLER_BIN="/usr/bin/plugins/mos-nethogs-install"

run_nethogs_install() {
  if [[ -x "$INSTALLER_BIN" ]]; then
    "$INSTALLER_BIN" || true
  fi
}

install() {
  run_nethogs_install
}

plugin_update() {
  run_nethogs_install
}

mos_start() {
  run_nethogs_install
}

mos_osupdate() {
  run_nethogs_install
}

uninstall() {
  true
}